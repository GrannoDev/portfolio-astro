---
// --- Enter Your Birth Date Below ---
const BIRTH_YEAR = 1997;
const BIRTH_MONTH_INDEX = 6; // IMPORTANT: Month is 0-indexed (0=Jan, 1=Feb, ..., 11=Dec)
const BIRTH_DAY = 4; // Day of the month (1-31)
// ---------------------------------
const today = new Date();
const birthDate = new Date(BIRTH_YEAR, BIRTH_MONTH_INDEX, BIRTH_DAY);

// Calculate the initial age difference in years
let age = today.getFullYear() - birthDate.getFullYear();

// Create a date object representing this year's birthday
const birthdayThisYear = new Date(today.getFullYear(), BIRTH_MONTH_INDEX, BIRTH_DAY);

// Check if the current date is *before* this year's birthday
// If it is, the person hasn't had their birthday yet this year, so subtract 1 from the age.
if (today < birthdayThisYear) {
  age--;
}
---

<section id="about" class="container scroll-mt-20 w-fit mx-auto">
  <h2 class="max-lg:text-2xl text-3xl mb-8 font-bold text-center">About Me</h2>
  <div
    class="flex flex-col p-4 gap-4 bg-base-200 shadow-md shadow-base-100 rounded-md w-full backdrop-blur-lg opacity-80">
    <div>
      <p class="font-semibold opacity-80 text-primary text-sm">Name:</p>
      <p class="font-semibold text-2xl">Magnus Granno</p>
    </div>
    <div>
      <p class="font-semibold opacity-80 text-primary text-sm">Age:</p>
      <p class="font-semibold text-2xl">{age}</p>
    </div>
    <div>
      <p class="font-semibold opacity-80 text-primary mb-2 text-sm">Nationality:</p>
      <svg width="48" xmlns="http://www.w3.org/2000/svg" class="rounded-md" viewBox="0 0 37 28">
        <path fill="#c8102e" d="M0 0h37v28H0Z"></path><path stroke="#fff" stroke-width="4" d="M0 14h37M14 0v28"></path>
      </svg>
    </div>

    <div>
      <p class="font-semibold opacity-80 text-primary text-sm">Bio:</p>
      <p class="text-xl italic max-w-prose">
        Hi! I'm passionate about constantly learning and improving my skills, always striving to outdo my
        yesterday-self. Outside of the tech world, I enjoy the patience of fishing, the epic stories of Marvel, and the
        challenge and fun of gaming.
      </p>
    </div>
  </div>
</section>
